// radar-card.tsx — Stub placeholder for the radar card component.
// T0B.2 will implement the full component by refactoring the inline
// radar JSX from now.tsx into this standalone card.
// This stub exists only to satisfy the lazy import in card-registry.ts
// so that `tsc --noEmit` passes before T0B.2 runs.

import type { CardComponentProps } from '../../lib/card-registry';

export function RadarCard(_props: CardComponentProps) {
  return null; // Placeholder — T0B.2 will implement the full component
}

export default RadarCard;
